<canvas 
 class="onekit-canvas {{onekitClass}}"
 canvas-id="{{id}}" 
 id="{{onekitId}}"
 width="{{width}}" 
 height="{{height}}" 
 style="width:{{width}}rpx;height:{{height}}rpx;{{onekitStyle}}">
</canvas>