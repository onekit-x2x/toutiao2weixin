
<onekit-view onekit-class="container">
    <onekit-view onekit-class="body">
            <onekit-view onekit-class="page-section">
                <block wx:if="{{hasLogin === true}}">
                    <onekit-text onekit-class="page-body-title">已登录</onekit-text>
                    <onekit-text onekit-class="page-body-text">每个用户仅需登录 1 次，后续每次进入页面即可根据 ttid 自动拉取用户信息</onekit-text>
                    <onekit-text onekit-class="page-body-text" wx:if="{{code !== ''}}">code: {{code}}</onekit-text>
                </block>
                <block wx:if="{{hasLogin === false}}">
                    <onekit-text onekit-class="page-body-text">未登录 或 当前 session 已经失效，需要重新登录</onekit-text>
                    <onekit-button onekit-class="page-body-button" type="primary" bindtap="login">登录</onekit-button>
                </block>
            </onekit-view>
    </onekit-view>
</onekit-view>